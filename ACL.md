# ACL.md — ตารางสิทธิ์ผู้ใช้ระบบ LeaveEasy

> อ้างอิงจาก `leaveeasy-spec.md` §2 (ผู้ใช้ 3 บทบาท) — เอกสารนี้สรุปมาให้ดูง่ายเป็นตารางเดียว
> ค่าที่เก็บในช่อง `role` ของ Firestore ต้องใช้รหัสภาษาอังกฤษตามนี้เสมอ (`employee` / `manager` / `hr`)

| บทบาท | ทำได้ | ทำไม่ได้ |
|---|---|---|
| 🙋 **พนักงาน** (`employee`) | ยื่นใบขอลาใหม่ · ดูใบลาของตัวเอง · เขียนความเห็นเพิ่มในใบของตัวเอง · ลบใบของตัวเองที่สถานะยังเป็น `รอพิจารณา` | **อนุมัติ/ไม่อนุมัติใบลาของตัวเองไม่ได้** (เปลี่ยนสถานะใบลาไม่ได้เลย ไม่ว่าใบใคร) · **ดูใบลาของคนอื่นไม่ได้** · แก้ไขประเภทการลาไม่ได้ |
| ✅ **หัวหน้า** (`manager`) | ดูใบลาทุกใบ · เปลี่ยนสถานะใบลา (อนุมัติ / ไม่อนุมัติ) · เขียนความเห็นในทุกใบ | ลบใบลาของคนอื่น · แก้ไขประเภทการลา |
| 🧾 **ฝ่ายบุคคล** (`hr`) | ทำได้ทุกอย่างที่หัวหน้าทำได้ · กำหนดผู้อนุมัติให้แต่ละใบ · เพิ่ม/แก้/ลบ ประเภทการลา · เห็นแดชบอร์ดสรุปทั้งระบบ | — (ไม่มีข้อจำกัดเพิ่มเติม) |

---

## ⚠️ สถานะการบังคับใช้จริงตอนนี้

- **ฝั่งหน้าจอ (UI) บังคับตามตารางนี้**: `js/nav-user.js` ซ่อนเมนู "ประเภทการลา" จากคนที่ไม่ใช่ hr, `leave-types.js` กันไม่ให้ non-hr เข้าหน้านั้นได้แม้พิมพ์ URL ตรงๆ, `js/leave-requests.js` กรองตารางให้พนักงานเห็นเฉพาะใบตัวเอง, `js/leave-request-detail.js` บล็อกพนักงานที่เปิดใบของคนอื่นด้วย URL ตรงๆ และซ่อนปุ่มอนุมัติ/ไม่อนุมัติจากพนักงาน (ปุ่มลบใบลาอิงความเป็นเจ้าของ ไม่อิง role — ใครก็ลบใบตัวเองที่ยังรอพิจารณาได้ ไม่ใช่แค่พนักงาน)
- **✅ ตั้งแต่สัปดาห์ที่ 8 ตารางนี้บังคับจริงฝั่ง Firestore Security Rules แล้ว** (`firestore.rules`, deploy แล้วขึ้น `leaveeasy-jakrapan`): พนักงานที่เปิด DevTools เรียก Firestore SDK ตรงๆ ข้ามหน้าเว็บไปเลย จะ**อ่าน/แก้ใบลาของคนอื่นไม่ได้อีกต่อไป** (ได้ `permission-denied` ทันที), แก้ประเภทการลาไม่ได้ถ้าไม่ใช่ hr, ตั้ง role ตัวเองตอนสมัครเป็นอย่างอื่นนอกจาก employee ไม่ได้ — ทดสอบด้วยการสวมรอย (ล็อกอินเป็น employee คนหนึ่ง แล้วลองเปิด `leaveRequests/{id}` ของอีกคนตรงๆ ทาง URL) แล้วยืนยันว่าถูกปฏิเสธจริง
- **สิ่งที่ยังเป็น business rule ฝั่ง client เท่านั้น ไม่ได้ย้ายไป Rules**: กฎ "ไม่อนุมัติได้ต้องมีความเห็นอย่างน้อย 1 รายการก่อน" (เช็คใน `เปลี่ยนสถานะ()` — การนับจำนวนเอกสารใน subcollection ทำใน Security Rules ได้ยาก/แพง) และการส่งความเห็นใหม่ (`ส่งความเห็น`) ที่ยังไม่เขียนกลับ Firestore เลย (ดู CLAUDE.md ส่วน Migration state) จึงยังไม่มีอะไรให้ Rules คุ้มครองตรงนั้น

ตารางนี้เป็นทั้ง **ข้อกำหนด (spec)** ที่ `firestore.rules` เขียนตามจริง และคำอธิบายพฤติกรรม UI ไปพร้อมกัน — แก้ตารางนี้เมื่อไร ต้องเช็คว่า `firestore.rules` ยังตรงกันอยู่ด้วย
