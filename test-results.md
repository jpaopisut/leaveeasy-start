# ผลทดสอบ LeaveEasy (Playwright MCP)

**รันล่าสุด:** 2026-09-20 เวลา 13:18–13:22 น. (ไทย, UTC+7)
**สภาพแวดล้อม:** dev server `npm run dev` (`http://localhost:59124`) เทียบกับ Firestore project จริง `leaveeasy-jakrapan` (ไม่ใช้ emulator)
**บัญชีทดสอบที่ใช้** (รหัสผ่านไม่แสดงในไฟล์นี้ เพราะเป็นบัญชีจริงบนเว็บ production — repo นี้เป็นสาธารณะ):
| Email | Role |
|---|---|
| `test-employee@leaveeasy.test` | employee |
| `test-employee2@leaveeasy.test` | employee |
| `test-manager@leaveeasy.test` | manager |
| `test-hr@leaveeasy.test` | hr |

---

## สรุปผล

| # | เทสต์ | ทดสอบอะไร | ผล | เวลารัน |
|---|---|---|:---:|---|
| 1 | ยื่นใบลาแล้วเห็นในรายการ | ยื่นใบลาใหม่ (employee) → เขียนลง Firestore จริง → ปรากฏในตารางรายการ แม้รีโหลดหน้า | ✅ ผ่าน | 13:18–13:19 |
| 2 | กดอนุมัติแล้วสถานะเปลี่ยน | manager เปิดใบที่ `รอพิจารณา` → กดอนุมัติ → สถานะเปลี่ยนเป็น `อนุมัติ` และคงอยู่แม้รีโหลดหน้า | ✅ ผ่าน | 13:19–13:21 |
| 3 | เว้นหัวข้อว่างแล้วบันทึก | กรอกฟอร์มยื่นใบลาครบทุกช่องยกเว้น "หัวข้อ" แล้วกดบันทึก → ต้องไม่บันทึกและต้องมีข้อความเตือน | ✅ ผ่าน | 13:21 |
| 4 | ไม่ล็อกอินแล้วเปิดหน้ารายการ | ไม่มี session → เปิด `leave-requests.html` ตรงๆ → ต้องอ่านข้อมูลไม่ได้ (ทั้งที่ UI redirect และที่ระดับ Firestore Security Rules) | ✅ ผ่าน | 13:20 |
| 5 | บัญชีที่สองเปิดใบลาของบัญชีแรก | ล็อกอินบัญชี B แล้วเปิด `leave-request-detail.html?id=...` ของบัญชี A ตรงๆ ทาง URL → ต้องเปิดไม่ได้ | ✅ ผ่าน | 13:20 |

**ผลรวม: 5/5 ผ่าน — ไม่มีเทสต์ไหนติด ไม่มีบั๊กให้แก้ในรอบนี้**

---

## รายละเอียดแต่ละเทสต์

### 1. ยื่นใบลาแล้วเห็นในรายการ — ✅ ผ่าน
1. ล็อกอิน `test-employee@leaveeasy.test`
2. เปิด `new-leave-request.html` กรอกหัวข้อ `PWTEST-RUN2-ยื่นใบลา`
3. กดบันทึก → redirect ไป `leave-requests.html` ไม่มี console error
4. เจอแถวหัวข้อที่ตั้งไว้ในตาราง
5. รีโหลดหน้า แถวยังอยู่ (ยืนยันเขียน Firestore จริงผ่าน `addDoc`)

### 2. กดอนุมัติแล้วสถานะเปลี่ยน — ✅ ผ่าน
1. ล็อกอิน employee ยื่นใบลาใหม่ `PWTEST-RUN2-อนุมัติ` (สถานะเริ่มต้น `รอพิจารณา`)
2. อ่าน `data-id` ของแถวที่สร้าง (`g4Bxq4O66RUSAqpUOorV`)
3. ออกจากระบบ → ล็อกอิน `test-manager@leaveeasy.test`
4. เปิด `leave-request-detail.html?id=...` ตรงๆ → เห็นสถานะ `รอพิจารณา` และปุ่มอนุมัติ/ไม่อนุมัติ
5. กดอนุมัติ → สถานะเปลี่ยนเป็น `อนุมัติ` ปุ่มหายไป ไม่มี console error
6. รีโหลดหน้า สถานะยังเป็น `อนุมัติ` (ยืนยันเขียน Firestore จริงผ่าน `updateDoc`)

### 3. เว้นหัวข้อว่างแล้วบันทึก — ✅ ผ่าน
1. ล็อกอิน employee เปิด `new-leave-request.html`
2. กรอกทุกช่องยกเว้น "หัวข้อ" (เว้นว่าง) แล้วกดบันทึก
3. ระบบไม่ redirect ยังอยู่หน้าเดิม ขึ้นข้อความ `⚠️ กรอกไม่ครบ — ต้องกรอกทุกช่องก่อนกดบันทึก`
4. เช็ค `leave-requests.html` ไม่มีแถวหัวข้อว่างเกิดขึ้น (validation ฝั่ง client ดักไว้ก่อนเรียก Firestore จริง)

### 4. ไม่ล็อกอินแล้วเปิดหน้ารายการ — ✅ ผ่าน
1. ออกจากระบบ (ไม่มี session)
2. เปิด `leave-requests.html` ตรงๆ → ระบบ redirect ไป `login.html` ทันที ไม่มีข้อมูลใบลาโผล่มา
3. เช็คเพิ่มระดับ Security Rules: เรียก Firestore SDK ตรงๆ ผ่าน console ขณะไม่ล็อกอิน → ได้ `permission-denied: Missing or insufficient permissions.`

### 5. บัญชีที่สองเปิดใบลาของบัญชีแรก — ✅ ผ่าน
1. ล็อกอินบัญชีแรก (`test-employee`) จดไอดีใบลาที่เป็นเจ้าของ (`fSmJnzskVZAhzNJYgB5A`)
2. ออกจากระบบ → ล็อกอินบัญชีที่สอง (`test-employee2`, role employee เหมือนกัน)
3. เปิด `leave-request-detail.html?id=...` ของบัญชีแรกตรงๆ → ขึ้นข้อความ "คุณไม่มีสิทธิ์ดูใบลานี้ — เปิดดูใบลาของคนอื่นไม่ได้" ไม่มีข้อมูลใบลาโผล่มาเลย

---

## หมายเหตุ

- ไม่มีเทสต์ไหนไม่ผ่านในรอบนี้ จึงไม่มีอะไรต้อง "บอกว่าติดตรงไหน"
- เทสต์ 1–3 และ 5 รันซ้ำได้เรื่อยๆ โดยไม่ทิ้งข้อมูลค้าง ยกเว้นเทสต์ 2 ที่จบด้วยสถานะ `อนุมัติ` (ลบผ่านแอปไม่ได้ตามการออกแบบ state machine ทางเดียว) — ยอมรับขยะสะสมนี้ไว้ตามที่ตกลงกันไว้ก่อนหน้า ถ้าต้องการล้างต้องลบผ่าน Firebase Console
- ทุกเทสต์รันเทียบกับ Firestore project จริง (`leaveeasy-jakrapan`) ไม่ใช้ emulator
